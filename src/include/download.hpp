#ifndef DOWNLOAD_HPP
#define DOWNLOAD_HPP

bool download_song_from_query(const std::string& query);
bool _archive_org_download_song_from_query(int new_id, const std::string& title, const std::string& artist);

#endif
